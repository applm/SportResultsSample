query GetListOfSportResults {
  listSportResults {
    uid,
    name,
    place,
    duration
  }
}

mutation AddSportResult($sportResultInput: SportResultInput!) {
  addSportResult(sportResultInput : $sportResultInput){
    uid
  }
}